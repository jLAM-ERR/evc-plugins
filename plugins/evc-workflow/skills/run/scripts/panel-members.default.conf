# panel-members.default.conf — design-panel members as flat `tier.stage=role,role` lines.
# To change composition per project, copy this to `.evc-workflow/panel-members.conf` and edit.
# Quick has no design stage, so it has no line here. intake runs as a single agent, not a panel.
standard.design=architect,qa
deep.design=architect,security,qa,dba
